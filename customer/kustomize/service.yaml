apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: customer
spec:
  template:
    metadata:
      name: customer-v1
    spec:
      containers:
      - image: changeonbuild:changeonbuild
        resources:
          limits:
            cpu: "1"